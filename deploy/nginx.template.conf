server {
    listen 80;
    server_name SITENAME;

    location / {
        root /var/www/html;

        location ~* /([a-z\-)]+)/ {
            try_files $uri /$1/index.html /index.html;
        }

        try_files $uri $uri/index.html /index.html;
    }
}
